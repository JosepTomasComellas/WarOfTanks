version: '3.8'

# ─────────────────────────────────────────────
#  War of Tanks — CLIENT (alumnes)
#  Crea un fitxer .env amb: SERVER_IP=x.x.x.x
#  Després: docker compose up -d
# ─────────────────────────────────────────────

services:
  war-of-tanks:
    image: ghcr.io/joseptomascomellas/warofttanks:latest
    container_name: wot-client
    environment:
      - ROLE=client
      - SERVER_IP=${SERVER_IP}
      - UDP_PORT=8888
      - HTTP_PORT=8080
    ports:
      - "8080:8080"
    restart: unless-stopped
